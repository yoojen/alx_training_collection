#include "main.h"
/**
 * int _abs : make an absolute value of number
 * return: integer
 * r: parameter
 */

int _abs(int r)/* _abs returns integer*/
{
	if (r < 0)
	{
		return (r * (-1));
	}
	return (r);
}
